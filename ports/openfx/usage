The package OpenFX provides CMake targets:

    find_package(unofficial-openfx CONFIG REQUIRED)
    # C-based API
    target_link_libraries(main PRIVATE unofficial::openfx::OpenFx-standard)
    # C++ wrapper
    target_link_libraries(main PRIVATE  unofficial::openfx::OfxSupport-standard)
    # OfxHost
    target_link_libraries(main PRIVATE  unofficial::openfx::OfxHost)
