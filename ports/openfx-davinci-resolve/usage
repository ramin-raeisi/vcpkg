The package OpenFX provides CMake targets:

    find_package(unofficial-openfx-davinci-resolve CONFIG REQUIRED)
    # C-based API
    target_link_libraries(main PRIVATE unofficial::openfx-davinci-resolve::OpenFx)
    # C++ wrapper
    target_link_libraries(main PRIVATE unofficial::openfx-davinci-resolve::OfxSupport)
